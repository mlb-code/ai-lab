# הוראה לסשן האתר: "פתיחת חשבון חינם" בדף הבית (13.09.2026)

**מאת:** סשן השיווק. **אישור:** מאיר (13.09). **קובץ:** `index.html` בלבד (דף הנחיתה `/parents/` מטופל על ידי סשן השיווק, לא לגעת). **לא לגעת:** טקסטים אחרים, סקשנים אחרים, האנימציות, האירועים הקיימים.

## למה
254 ביקורים ביומיים (12–13.09), כמעט כולם ממודעות, ולחיצה אחת בלבד על "הרשמה". בהירו במובייל יש 5 כפתורים (שאלו את ה-AI, דיסקורד, קהילת וואטסאפ, מפת הקורסים, תג דירוג) ואף אחד לא מוביל להרשמה. עמוד ההצטרפות (my.ai-lab.co.il/join) כבר מבטיח "פתיחת החשבון חינם לגמרי ובלי התחייבות, משלמים רק כשבוחרים קורס", וזה המסר שצריך להופיע כבר בהירו.

## מה לשנות

### 1. הירו, שורת הכפתורים (`.cr-row` בסקשן ההירו, סביב שורה 2733)
- **כפתור ראשי חדש:** `פתיחת חשבון חינם` → `https://my.ai-lab.co.il/join` (אותו עיצוב של `btn btn-primary` + `holo-wrap` שיש היום ל"למפת הקורסים"). לשמור על העברת ה-UTM אם יש מנגנון כזה בדף (ב-/parents יש; אם אין בדף הבית, לא להוסיף עכשיו).
- **"למפת הקורסים"** הופך לכפתור משני (`btn btn-ghost-dark` או המקבילה בהירו), נשאר עוגן `#courses`.
- **"הוואטסאפ שלנו"** (הקישור ל-wa.me עם `data-cta="whatsapp_hero"`) נשאר כמו שהוא.
- **תג הדירוג 5.0** נשאר.
- **כפתורי "הקהילה בדיסקורד" ו"הקהילה בוואטסאפ"** (מוצגים במובייל בהירו לפי הסדר ב-CSS סביב שורה 2542) יוצאים מההירו. הם כבר קיימים בסקשן הקהילה (`#community`), שם הם נשארים. כלומר בהירו במובייל: ראשי "פתיחת חשבון חינם" → "למפת הקורסים" → "הוואטסאפ שלנו" → תג 5.0. הכפתור "אני ה-AI של המרכז" נשאר.
- מתחת לכפתור הראשי, שורה קטנה (13–14px, צבע משני): `חינם ובלי התחייבות · משלמים רק כשבוחרים קורס`.

### 2. שורת כפתורים צפה במובייל (חדש)
- רק במסכים עד 640px. מופיעה אחרי שגוללים מעבר להירו (אותו טריגר של ה-orb הקיים, `IntersectionObserver` על `heroEl`, סביב שורה 3979), נעלמת כשההירו בתצוגה.
- שני כפתורים ברוחב מלא, מוצמדים לתחתית המסך, מעל כל תוכן, עם רקע כהה חצי-שקוף וטשטוש (אותה שפה ויזואלית של האתר): `פתיחת חשבון חינם` (ראשי, → `https://my.ai-lab.co.il/join`) ו-`וואטסאפ` (ירוק, → אותו קישור wa.me של ההירו עם ההודעה המוכנה).
- לא לחפוף לכפתור הוואטסאפ הצף אם קיים בדף הבית (אם קיים, השורה מחליפה אותו במובייל).
- `padding-bottom` לגוף הדף במובייל כדי שהפוטר לא יוסתר מאחורי השורה.

### 3. כרטיסי הקורסים (`#courses`)
- טקסט כפתור ההרשמה בכרטיס: `פרטים והרשמה` (לא "הרשמה חינם", הקורס בתשלום).
- מתחת לרשת הכרטיסים, שורה אחת: `פתיחת חשבון חינם, משלמים רק כשבוחרים מחזור.` עם קישור ל-`https://my.ai-lab.co.il/join`.

### 4. פוטר, `foot-cta` ("הילד שלכם הבא בתור?")
- ליד "שאלו אותנו בוואטסאפ" הכפתור `להרשמה ←` (id `mRegister`) משנה טקסט ל-`פתיחת חשבון חינם ←`. הקישור נשאר `/join`.

## מדידה (חובה, אותו מנגנון קיים)
כל הכפתורים החדשים שולחים `cta_click` כמו שאר הכפתורים בדף, עם `cta_section` ו-`cta_label` ברורים:
- הירו: `cta_section=hero`, `cta_label=free_signup`
- שורה צפה: `cta_section=sticky`, `cta_label=free_signup` / `whatsapp`
- קורסים: `cta_section=courses`, `cta_label=free_signup_note`
- פוטר: `cta_section=foot`, `cta_label=free_signup`
סשן השיווק ישווה אחרי 5 ימים (לחיצות → חשבונות שנפתחו בפלטפורמה).

## מה לבדוק לפני פריסה
מובייל 375px ומחשב: ההירו לא נשבר, השורה הצפה לא מסתירה תוכן, הפוטר נראה, אין שגיאות JS (`agent/visual-check.mjs` כבר יודע לצלם). לפרוס ל-main (GitHub Pages), ולדווח לסשן השיווק בקובץ `דיווח-הרשמה-חינם-<תאריך>.md`: מה שונה, ואישור שהאירועים נשלחים.
