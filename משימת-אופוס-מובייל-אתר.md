# כרטיס משימה לאופוס 5 — השלמת מובייל לאתר החדש

> הדבק את כל הקובץ הזה כהודעה ראשונה בסשן חדש אחרי `/model opus[1m]`, מהתיקייה `/Users/meirlb/Desktop/ai-lab`.

## המשימה
להשלים את התאמת המובייל של `redesign-2026.html` — האתר החדש של ai-lab (ליקוויד-גלאס). ההירו, הערימה, הקרוסלות והתפריט כבר הותאמו; נשארו הפאנלים הפנימיים ומודאל הסילבוס.

## חוקים מחייבים (אין חריגות)
1. עובדים **רק** על `/Users/meirlb/Desktop/ai-lab/redesign-2026.html`. לא נוגעים בשום קובץ אחר, לא פורסים לשום מקום, לא נוגעים בגיט.
2. כל עריכה: סקריפט python עם `assert s.count(old) == 1` לפני replace, ואחרי כל עריכה להריץ אימות JS:
   `node -e "const s=require('fs').readFileSync('redesign-2026.html','utf8');[...s.matchAll(/<script>([\s\S]*?)<\/script>/g)].forEach((x,i)=>{new Function(x[1])});console.log('JS OK')"`
3. שרת מקומי כבר רץ: http://localhost:5502/redesign-2026.html (אם נפל: דאבל-קליק על `start-server.command` בתיקייה, או `node dev-server.mjs`).
4. אימות חזותי עם puppeteer (מותקן ב-`/Users/meirlb/Desktop/ai-lab/node_modules/puppeteer`) בהדמיית אייפון:
   `await pg.emulate({viewport:{width:390,height:844,deviceScaleFactor:2.5,isMobile:true,hasTouch:true}, userAgent:'Mozilla/5.0 (iPhone) AppleWebKit/605.1.15 Version/17.5 Mobile Safari/604.1'})`
   פתיחת פאנל: `document.querySelectorAll('.hub-head')[i].click()` (0=פרויקטים 1=סרטונים 2=אזור אישי 3=קהילה 4=שאלות). גלילה: `pg.mouse.wheel` (לא scrollTo).
5. שינויי מובייל נכנסים **רק בתוך** בלוק `@media (max-width: 960px)` הקיים (או 560). אסור לשנות התנהגות דסקטופ.
6. שפת העיצוב: הזכוכית הקיימת בקובץ. בלי צבעים חדשים, בלי ספריות, בלי פונטים חדשים.
7. טקסטים: לא לשנות ניסוחים. `text-wrap: balance/pretty` כבר גלובלי.
7ב. **לא נוגעים בקוד הסוכן** (הפונקציה `agent()` וה-fetch ל-my.ai-lab.co.il שבתוכה) — הוא חובר למוח אמיתי ועובד. מותר CSS למראה שלו במובייל בלבד.
8. בסוף כל תת-משימה: צילום לפני/אחרי ובדיקת `document.documentElement.scrollWidth - innerWidth === 0` (אפס גלישה אופקית).

## תת-המשימות
### 1. פאנל "האזור האישי ואיך זה עובד" במובייל
מוקאפ הטלפון + הצ'יפים הצפים + שלושת הצעדים + קרוסלת הביקורות — לוודא: טור אחד נקי, המוקאפ לא נחתך, הצ'יפים לא מתנגשים בטקסט, הביקורת קריאה עם הנקודות.

### 2. פאנל "קהילה ובלוג" במובייל
כרטיסי וואטסאפ/דיסקורד במלוא הרוחב, כרטיסי הבלוג בקרוסלת snap (כבר קיימת — לוודא שעובדת), כפתור "לכל המאמרים" נגיש.

### 3. פאנל "שאלות נפוצות" במובייל
האקורדיון במלוא רוחב המסך פחות שוליים, פתיחת תשובה לא שוברת פריסה, כפתור "לכל השאלות" בולט.

### 4. מודאל הסילבוס במובייל
נפתח מכרטיסי הקורסים בסקשן העצמאי. במובייל: מסך כמעט מלא (עד ~95svh), גלילה פנימית חלקה, כפתור סגירה נגיש באגודל, כפתורי ההרשמה/וואטסאפ בתחתית לא מוסתרים. בדיקה: לפתוח דרך `[...document.querySelectorAll('.c-more')].filter(e=>e.offsetParent)[0].click()` אחרי גלילה לסקשן הקורסים.

### 5. סקשן הקורסים העצמאי במובייל
כותרת, פס המיומנויות, כרטיסי המחזורים (קרוסלה קיימת), החפיסות 2×2 והמניפה (קרוסלה קיימת) — לוודא ריווחים נכונים ושום דבר לא נחתך על 360/390/430.

### 6. בדיקת סיום כוללת
מעבר מלא על העמוד ב-360, 390 ו-430 רוחב: אפס גלישה אופקית, אפס אלמנטים חתוכים, צילומי מסך של כל פאנל פתוח. דו"ח קצר בעברית: מה שונה, מה נבדק.

## קריטריון הצלחה
מאיר פותח את http://localhost:5502/redesign-2026.html בתצוגת אייפון, פותח כל פאנל, פותח סילבוס — והכול נראה מקצועי כמו ההירו והערימה שכבר גמורים. שום שינוי לא נראה בדסקטופ.
