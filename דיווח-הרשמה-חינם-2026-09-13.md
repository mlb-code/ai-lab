# דיווח: "פתיחת חשבון חינם" בדף הבית — פרוס 13.09.2026

**מאת:** סשן האתר/הפלטפורמה. **קובץ:** `index.html` בלבד (commit `ad39f1c`, GitHub Pages, חי ואומת ב-curl). `/parents/` לא נגעתי.

## מה שונה
1. **הירו:** כפתור ראשי `פתיחת חשבון חינם ←` → `https://my.ai-lab.co.il/join` (btn-primary + holo-wrap). מתחתיו שורה: "חינם ובלי התחייבות · משלמים רק כשבוחרים קורס". `למפת הקורסים` הפך למשני (`btn-ghost-dark`, עוגן `#courses`). `הוואטסאפ שלנו` ותג 5.0 נשארו. **כפתורי "הקהילה בוואטסאפ"/"הקהילה בדיסקורד" הוסרו מההירו** (נשארו ב-`#community`). "אני ה-AI של המרכז" נשאר.
   סדר במובייל (≤960): AI → פתיחת חשבון חינם → הערה → למפת הקורסים → הוואטסאפ שלנו → תג 5.0.
   (הערה: "הוואטסאפ שלנו" היה מוסתר במובייל מ-10.09; לפי ההוראה הוא מוצג עכשיו גם במובייל, ברוחב מלא.)
2. **שורה צפה במובייל:** השורה הקיימת (`.mobile-bar`, ≤960px) הוחלפה ל-`פתיחת חשבון חינם` (ראשי) + `וואטסאפ` (ירוק, אותו wa.me + הודעה כמו בהירו). מופיעה כשגוללים מעבר לכפתורי ההירו, נעלמת כשהם בתצוגה. `body` כבר עם padding-bottom והפוטר עם ריווח — אומת שהשורה המשפטית לא מוסתרת. **"דברו איתי" (AI) ירד מהשורה** — במובייל הגישה ל-AI היא מההירו (השורה הצפה החליפה אותו; כדור הסוכן מוסתר במובייל מלכתחילה).
3. **`#courses`:** כפתור הכרטיס = `פרטים והרשמה`. מתחת לרשת: "**פתיחת חשבון חינם**, משלמים רק כשבוחרים מחזור." (קישור ל-/join).
4. **פוטר (`foot-cta`):** `להרשמה ←` → `פתיחת חשבון חינם ←` (ל-/join). (ההוראה ציינה id `mRegister` — זה כפתור המודאל של הקורס, לא נגעתי בו; "להרשמה" שם נשאר.)
- UTM: אין מנגנון העברה בדף הבית — לא נוסף (לפי ההוראה).

## מדידה — אומת מקומית (dataLayer, אחרי עקיפת חסימת webdriver)
`cta_click` נשלח עם:
`hero / free_signup` · `hero / see_courses` · `hero / הוואטסאפ שלנו` · `sticky / free_signup` · `sticky / whatsapp` · `courses / free_signup_note` · `foot / free_signup`.
(נוסף תמיכה ב-`data-section` במנגנון הקיים; שאר הכפתורים בדף ללא שינוי.)

## בדיקות
375px ו-1280px: אין שגיאות JS, אין גלילה אופקית, ההירו לא נשבר, השורה הצפה לא מסתירה תוכן, הפוטר נראה. צילומים: hero/scrolled/footer בשני הרוחבים.
